# ResultDisplay

`ResultDisplay` renders the markdown output of the generated fire safety report.

## Props

- `result`: The markdown string to render.
- `onAskAssistant`: Callback to open the expert assistant modal.

## Usage

```tsx
<ResultDisplay result={reportContent} onAskAssistant={() => {}} />
```
