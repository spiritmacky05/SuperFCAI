# EmbedModal

`EmbedModal` provides users with an iframe snippet to embed the Super FC AI assistant into external websites.

## Props

- `isOpen`: Boolean to control visibility.
- `onClose`: Callback to close the modal.

## Usage

```tsx
<EmbedModal isOpen={true} onClose={() => {}} />
```
