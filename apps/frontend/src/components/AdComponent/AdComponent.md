# AdComponent

A basic Google AdSense unit component used for general ad placement.

## Component

Uses the standard `ins.adsbygoogle` pattern with an automatic format.

## Usage

```tsx
<AdComponent />
```
